name: Spec 10 Canary (DO NOT MERGE — expected to fail)
on:
  pull_request:
    paths:
      - '.github/workflows/spec10-canary.yml'
jobs:
  canary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
